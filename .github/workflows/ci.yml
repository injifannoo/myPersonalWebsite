# name: Run Selenium Tests

# on:
#   push:
#     branches:
#       - main  # Run tests on the main branch when changes are pushed

# jobs:
#   test:
#     runs-on: ubuntu-latest

#     services:
#       selenium:
#         image: selenium/standalone-chrome
#         ports:
#           - 4444:4444

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Python
#         uses: actions/setup-python@v3
#         with:
#           python-version: '3.x'

#       - name: Install dependencies
#         run: |
#           python -m venv venv
#           source venv/bin/activate
#           pip install selenium pytest pytest-html

#       - name: Run Selenium tests
#         run: |
#           source venv/bin/activate
#           pytest --html=report.html --self-contained-html
          
#       - name: Upload report
#         uses: actions/upload-artifact@v3
#         with:
#           name: test-report
#           path: report.html

          
name: Run Selenium Tests CI

on:
  push:
    branches:
      - main  # Run tests on the main branch when changes are pushed

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      selenium:
        image: selenium/standalone-chrome  # Use Selenium Chrome standalone container
        ports:
          - 4444:4444

    steps:
      # Step 1: Check out code
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest-html webdriver-manager

      # Step 4: Run Selenium tests with HTML report
      - name: Run Selenium tests
        env:
          DISPLAY: ":99.0"  # Required for running Selenium in headless mode
        run: |
          pytest --html=report.html --self-contained-html

      # Step 5: Upload the test report as an artifact
      - name: Upload test report
        uses: actions/upload-artifact@v3
        with:
          name: selenium-test-report
          path: report.html
